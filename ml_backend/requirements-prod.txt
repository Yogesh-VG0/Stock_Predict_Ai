# Production minimal — CI/CD + FastAPI + StockPredictor
# Install: pip install -r requirements-prod.txt
#
# Heavy / unused packages REMOVED:
#   tensorflow, torch, xgboost, shap, optuna — not used by the pipeline
#   selenium, playwright, undetected-chromedriver, cloudscraper, selenium-stealth — need real browser, unusable in CI
#   tweepy — Twitter API not used
#   sentencepiece, transformers — FinBERT nice-to-have but adds ~2 GB; sentiment_cron guards with try/except
#   PyJWT, python-jose — no auth in the ML backend

# --- Core ---
fastapi>=0.104
uvicorn>=0.24
pydantic>=2.0
python-multipart>=0.0.6

# --- Data ---
pandas>=2.1
numpy>=1.26
scikit-learn>=1.3
lightgbm>=4.1
joblib>=1.3
yfinance
scipy>=1.11

# --- Database ---
pymongo>=4.6

# --- HTTP / async ---
httpx>=0.25
aiohttp>=3.9
requests>=2.31
tenacity>=8.2
backoff>=2.2

# --- Sentiment & finance ---
vaderSentiment>=3.3
feedparser>=6.0
praw>=7.7
finnhub-python>=2.4
fredapi>=0.5
beautifulsoup4>=4.12
pandas_market_calendars>=4.1

# --- AI / LLM APIs ---
google-genai>=1.0  # Gemini API
groq>=0.4.0        # Groq API (better free tier: 1K-14K RPD vs Gemini's 20)

# --- Misc ---
python-dotenv>=1.0
ratelimit>=2.2
brotlicffi>=1.1           # Brotli decompression for Finviz (pure-Python, no C compiler needed)
